#include "main.h"

/**
 * int_isupper - check the code
 * @c: the character to be checked
 * Return: 1 if c is uppercase
 * Return: 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c >= 'Z')
		return (1);
	else
		retunr (0);
}
